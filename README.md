# EVM Full-Stack Template

Repository scaffold for an EVM-related full-stack application.

Features:
- Python FastAPI backend with SQLAlchemy
- Ruff linting configuration
- pytest tests scaffold
- Simple frontend scaffold

Quickstart
---------
1. Create a virtual environment and activate it.
2. Install dependencies:

```
pip install -r requirements.txt
pip install -r dev-requirements.txt
```

3. Run the API:

```
make start
```

Pull Request
------------
Title: feat: Set up initial repository configuration and linter

Suggested commits:
- Add initial project structure
- Configure Ruff linter rules
- Add Makefile automation commands
